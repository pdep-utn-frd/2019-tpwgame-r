import wollok.game.*
import Personajes.*
import Configurar.*

program Bata11llaPorTurnos {
	
	/*
	 El enemigo solo ataca cuando el heroe termina de atacar
	 El heroe ataca, usa magia o cura
	 El ataque tiene que tener una probabilidad de critico(Mejorar la probabilidad, si es 0 es porque falla el ataque)
	 Cuando la vida de uno de ellos(el heroe o el enemigo) queden en 0, terminar el juego 
	*/
	juego.iniciar()
	game.start()
}









